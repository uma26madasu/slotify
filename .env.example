# Slotify Monorepo Environment Variables

# This file documents the environment variables needed for the monorepo.
# Each app (web and api) has its own .env file in its respective directory.

# See apps/web/.env.example for frontend environment variables
# See apps/api/.env.example for backend environment variables

# Root-level environment variables (if needed)
# NODE_ENV=development
