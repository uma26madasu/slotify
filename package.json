{
  "name": "slotify",
  "version": "1.0.0",
  "private": true,
  "description": "Slotify - Intelligent Scheduling Platform with AI-powered contextual augmentation",
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "scripts": {
    "dev": "turbo run dev",
    "build": "turbo run build --force",
    "start": "node apps/api/server.js",
    "lint": "turbo run lint",
    "vercel-build": "cd apps/web && npm run build && cd ../.. && rm -rf public && mv apps/web/dist public && touch public/index.js",
    "clean": "turbo run clean && rm -rf node_modules public",
    "web:dev": "npm --workspace=apps/web run dev",
    "api:dev": "npm --workspace=apps/api run dev",
    "web:build": "npm --workspace=apps/web run build",
    "api:build": "npm --workspace=apps/api run build"
  },
  "devDependencies": {
    "turbo": "^2.3.3"
  },
  "engines": {
    "node": ">=18.0.0",
    "npm": ">=9.0.0"
  }
}
