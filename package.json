{
  "name": "slotify",
  "version": "1.0.0",
  "private": true,
  "description": "Slotify - Intelligent Scheduling Platform with AI-powered contextual augmentation",
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "scripts": {
    "dev": "turbo run dev",
    "build": "turbo run build --force",
    "start": "node apps/api/server.js",
    "lint": "turbo run lint",
    "vercel-build": "turbo run build --filter=web && mkdir -p public && cp -r apps/web/dist/. public/""clean": "turbo run clean && rm -rf node_modules",
    "web:dev": "pnpm --filter web dev",
    "api:dev": "pnpm --filter api dev",
    "web:build": "pnpm --filter web build",
    "api:build": "pnpm --filter api build"
  },
  "devDependencies": {
    "turbo": "^2.3.3"
  },
  "engines": {
    "node": ">=18.0.0"
  },
  "packageManager": "pnpm@8.15.0"
}