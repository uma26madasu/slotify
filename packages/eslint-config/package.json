{
  "name": "eslint-config",
  "version": "1.0.0",
  "private": true,
  "description": "Shared ESLint configuration for Slotify monorepo",
  "main": "index.js",
  "scripts": {
    "clean": "rm -rf node_modules .turbo"
  }
}
