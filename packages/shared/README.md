# @slotify/shared

Shared utilities, types, and constants used across the Slotify monorepo.

## Usage

```javascript
import { dateUtils, validators, constants } from 'shared';
```

## Purpose

This package contains code that needs to be shared between the frontend (web) and backend (api) applications to maintain consistency and reduce duplication.
