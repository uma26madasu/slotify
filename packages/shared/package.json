{
  "name": "shared",
  "version": "1.0.0",
  "private": true,
  "description": "Shared utilities and types for Slotify monorepo",
  "main": "index.js",
  "scripts": {
    "build": "echo 'No build step required'",
    "clean": "rm -rf node_modules .turbo"
  }
}
