{
    "buildCommand": "npm install --prefix apps/web && npm run build --prefix apps/web",
    "outputDirectory": "apps/web/dist",
    "framework": null,
    "installCommand": "echo 'Skipping root install'",
    "rewrites": [
        {
            "source": "/(.*)",
            "destination": "/index.html"
        }
    ]
}