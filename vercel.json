{
    "buildCommand": "cd apps/web && npm run build && cd ../.. && mkdir -p public && cp -r apps/web/dist/. public/",
    "outputDirectory": "public",
    "framework": null,
    "installCommand": "npm install",
    "rewrites": [
        {
            "source": "/(.*)",
            "destination": "/index.html"
        }
    ]
}