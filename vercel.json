{
    "buildCommand": "cd ../../apps/web && npm install && npm run build && cd ../.. && pwd && ls -la && cp -r apps/web/dist dist && ls -la dist",
    "outputDirectory": "dist",
    "framework": null,
    "installCommand": "echo 'Skipping root install'",
    "rewrites": [
        {
            "source": "/(.*)",
            "destination": "/index.html"
        }
    ]
}